<div class="navigation">
	<% if user_signed_in? %>
		<%= link_to image_tag('howitworks_3.png', :size => '100x100', :class => 'left'), root_path %>
	<% end %>
	<div class="nav-inner">
		<% if user_signed_in? %>
			<%= link_to "Sign Out", destroy_user_session_path, :method => :delete, :class => 'btn btn-small btn-info right' %>
			<!-- <%= link_to "Products", destroy_user_session_path, :method => :delete, :class => 'btn btn-small btn-info right' %> -->

		<% end %>
	</div>
</div>