<%= form_for (@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field one_third left">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field one_fourth left">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
    <div class="field one_third left">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="clearfix"></div><br>
  <div class="field left one_half">
    <%= f.label :link %><br />
    <%= f.text_field :link %>
  </div>
    <div class="field left shift_one one_third">
    <%= f.label :photo %><br />
    <%= f.file_field :photo %>
  </div>
  <div class="clearfix"></div><br>
  <div class="field one_fourth left">
    <%= f.label :area_id %><br />
    <%= collection_select(:product, :area_id, @areas, :id, :name, { :include_blank => true }) %>
  </div>
  <div class="field one_third left">
    <%= f.label :category_id %><br />
    <%= collection_select(:product, :category_id, @categories, :id, :name, { :include_blank => true }) %>
  </div>
  <div class="field one_third left">
    <%= f.label :brand_id %><br />
    <%= collection_select(:product, :brand_id, @brands, :id, :name, { :include_blank => true }) %>
  </div>
  <div class="clearfix"></div><br>
<br>
  <div class="actions text-center">
    <%= f.submit 'Submit', class: "btn btn-inverse btn-large" %>
  </div>
<% end %>
